{
  "name": "astahelper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Assistente per l'asta del fantacalcio Classic, senza dipendenze",
  "scripts": {
    "test": "node --test 'test/*.test.js'",
    "build": "node scripts/build.js",
    "smoke": "node scripts/build.js && node scripts/smoke.js",
    "check": "npm test && npm run smoke",
    "simula": "node scripts/simula.js"
  }
}
